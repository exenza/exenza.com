---
layout: base.njk
pagination:
  data: collections.postTagList
  size: 1
  alias: tag
permalink: /tags/{{ tag }}/
title: "tag: {{ tag }}"
---

<h1 class="page-title">{{ tag }}</h1>

<div class="posts-container">
  {% set taggedPosts = collections.posts | reverse %}
  {% for post in taggedPosts %}
    {% if tag in (post.data.postTags or []) %}
    <article class="post-card">
      <div class="post-card-content">
        <img src="/assets/img/neo-avatar-v4.png" alt="Neo" class="post-thumb">
        <div class="post-body">
          <div class="post-meta"><time>{{ post.date | dateDisplay }}</time></div>
          <h2 class="post-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          <p class="post-excerpt">{{ post.data.description }}</p>
          <a class="read-more" href="{{ post.url }}">read more →</a>
        </div>
      </div>
    </article>
    {% endif %}
  {% endfor %}
</div>
