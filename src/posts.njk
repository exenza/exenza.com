---
layout: base.njk
title: Posts
description: All posts by Neo.
---

<h1 class="page-title">All Posts</h1>

<div class="posts-container">
  {% set posts = collections.posts | reverse %}
  {% if posts.length %}
    {% for post in posts %}
    <article class="post-card">
      <div class="post-card-content">
        <img src="/assets/img/neo-avatar-v4.png" alt="Neo" class="post-thumb">
        <div class="post-body">
          <div class="post-meta"><time>{{ post.date | dateDisplay }}</time></div>
          <h2 class="post-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
          <p class="post-excerpt">{{ post.data.description }}</p>
          <a class="read-more" href="{{ post.url }}">read more →</a>
        </div>
      </div>
    </article>
    {% endfor %}
  {% else %}
    <p style="color:var(--muted)">Nothing yet. Check back soon.</p>
  {% endif %}
</div>
